import {createHmac} from 'node:crypto';

const base=process.env.SMOKE_API_URL||'http://127.0.0.1:4000';
const bridgeSecret=process.env.NEXOFFICE_COMPLIANCE_BRIDGE_SECRET;if(!bridgeSecret)throw new Error('NEXOFFICE_COMPLIANCE_BRIDGE_SECRET required');
const password=process.env.SMOKE_USER_PASSWORD;if(!password)throw new Error('SMOKE_USER_PASSWORD required');
const assert=(value,message)=>{if(!value)throw new Error(`ASSERT: ${message}`)};

async function call(path,{method='GET',body,token,workspace,key,expect}={}){
  const headers={'content-type':'application/json'};if(token)headers.authorization=`Bearer ${token}`;if(workspace)headers['x-workspace-id']=workspace;if(key)headers['x-nexoffice-compliance-key']=key;
  const response=await fetch(base+path,{method,headers,body:body===undefined?undefined:JSON.stringify(body)});const payload=await response.json().catch(()=>({}));
  if(expect!==undefined){assert(response.status===expect,`${method} ${path}: expected ${expect}, got ${response.status} ${JSON.stringify(payload)}`);return{status:response.status,payload}}
  if(!response.ok)throw new Error(`${method} ${path} -> ${response.status} ${JSON.stringify(payload)}`);return payload;
}
async function register(suffix,businessName){const r=await call('/v1/auth/register',{method:'POST',body:{name:`Compliance ${suffix}`,email:`compliance-${suffix}-${Date.now()}@nexoffice.test`,password,businessName,vertical:'general'}});return{token:r.token,workspace:r.workspace.id,businessName}}

const a=await register('a','Empresa Compliance A');
await call('/v1/intelligence/profile',{method:'PUT',...a,body:{sector:'professional_services',subsector:'consultoria',revenueModel:'project',sellsServices:true}});
let overview=await call('/v1/compliance/overview',a);
assert(overview.available===true,'Compliance Hub is available');assert(overview.privacy?.mode==='aggregate_only','privacy mode is aggregate-only');assert(overview.legalGuidance?.automaticLegalConclusion===false,'triage never makes automatic legal conclusions');assert(overview.relevance?.state==='worth_checking','unknown company starts with neutral worth-checking state');assert(Array.isArray(overview.privacy?.forbidden)&&overview.privacy.forbidden.includes('psychosocial_answers'),'sensitive answer class is explicitly forbidden');assert(overview.nr1check?.configured===true&&overview.nr1check?.signedHandoff===true,'signed NR1Check handoff is configured');assert(overview.nr1check?.federated===false,'isolated smoke intentionally runs without NR1Check API federation');

const triage=await call('/v1/compliance/triage',{method:'PUT',...a,body:{hasEmployees:'yes',companyType:'other',hasSstSupport:'unknown',nr1ReviewStatus:'not_reviewed'}});assert(triage.relevance?.state==='review_recommended','employees + not reviewed raises product-level review recommendation');assert(triage.legalGuidance?.automaticLegalConclusion===false,'review recommendation is not legal conclusion');
const unsafeTriage=await call('/v1/compliance/triage',{method:'PUT',...a,body:{hasEmployees:'yes',companyType:'other',hasSstSupport:'unknown',nr1ReviewStatus:'not_reviewed',employeeName:'Pessoa X'},expect:400});assert(unsafeTriage.status===400,'triage rejects employee-level fields');

const handoff=await call('/v1/compliance/nr1check/handoff',{method:'POST',...a,body:{}});
assert(handoff.mode==='signed_fallback','safe signed fallback is used when passwordless federation is not configured');assert(handoff.passwordless===false,'fallback never pretends to be passwordless');assert(handoff.sensitiveBusinessDataShared===false,'handoff declares zero sensitive business sharing');assert(handoff.identityInBrowserUrl===false,'identity is never placed in browser URL');assert(handoff.businessContextOpaque===true,'browser business context is opaque');assert(handoff.sharedBusinessFields.join(',')==='workspaceRef,businessName,sector','handoff has exact safe business field allowlist');
const handoffUrl=new URL(handoff.url);const params=[...handoffUrl.searchParams.keys()].sort();assert(handoffUrl.origin==='https://nr1check.test','handoff uses configured NR1Check origin');assert(JSON.stringify(params)===JSON.stringify(['handoff']),'browser URL contains only opaque signed handoff');assert(!handoffUrl.searchParams.has('workspaceRef')&&!handoffUrl.searchParams.has('businessName')&&!handoffUrl.searchParams.has('sector')&&!handoffUrl.searchParams.has('email'),'raw business and identity fields are absent from URL');
const token=handoffUrl.searchParams.get('handoff');const [encoded,sig]=String(token||'').split('.');assert(encoded&&sig,'handoff token has payload and signature');const expected=createHmac('sha256',bridgeSecret).update(encoded).digest('base64url');assert(sig===expected,'handoff HMAC is valid');const signedPayload=JSON.parse(Buffer.from(encoded,'base64url').toString('utf8'));assert(signedPayload.v===1&&signedPayload.workspaceRef===a.workspace,'signed payload is versioned and tenant-bound');assert(signedPayload.businessName===a.businessName&&signedPayload.sector==='professional_services','signed payload carries safe known business context');assert(typeof signedPayload.nonce==='string'&&signedPayload.nonce.length>=8,'handoff includes nonce');assert(signedPayload.exp*1000>Date.now()&&signedPayload.exp*1000<=Date.now()+11*60_000,'handoff is short-lived');assert(!('email' in signedPayload)&&!('userId' in signedPayload)&&!('cpf' in signedPayload),'signed payload contains no identity or sensitive data');

await call('/v1/internal/compliance/summary',{method:'POST',key:'wrong-key',body:{workspaceRef:a.workspace,sourceProduct:'mindcompliance',openActions:2,overdueActions:1},expect:401});const unsafeSummary=await call('/v1/internal/compliance/summary',{method:'POST',key:bridgeSecret,body:{workspaceRef:a.workspace,sourceProduct:'mindcompliance',openActions:2,overdueActions:1,psychosocialAnswers:[{employee:'x',answer:'secret'}]},expect:400});assert(unsafeSummary.status===400,'aggregate ingestion rejects raw sensitive payloads');
await call('/v1/internal/compliance/summary',{method:'POST',key:bridgeSecret,body:{workspaceRef:a.workspace,sourceProduct:'mindcompliance',diagnosticStatus:'completed',programStatus:'attention',openActions:3,overdueActions:1,nextDueAt:new Date(Date.now()+3*86400000).toISOString(),completionPct:62,categories:['NR-1','Plano de ação'],deepLink:'https://nr1check.test/dashboard'}});
overview=await call('/v1/compliance/overview',a);assert(overview.mindcompliance?.connected===true,'aggregate compliance summary is connected');assert(overview.mindcompliance.summary.openActions===3&&overview.mindcompliance.summary.overdueActions===1,'only operational aggregate counts are surfaced');assert(!('psychosocialAnswers' in overview.mindcompliance.summary)&&!('employeeName' in overview.mindcompliance.summary),'sensitive fields never surface');
const executive=await call('/v1/intelligence/executive-brief',a);assert(executive.compliance?.privacy==='aggregate_only','executive briefing receives only aggregate compliance snapshot');assert(executive.compliance?.overdueActions===1,'executive briefing sees aggregate overdue count');assert(executive.attention?.target==='compliance','overdue compliance becomes a day-to-day attention item when no higher cash urgency exists');assert(!JSON.stringify(executive).includes('psychosocialAnswers'),'executive payload cannot expose raw psychosocial data');
const b=await register('b','Empresa Compliance B');const overviewB=await call('/v1/compliance/overview',b);assert(overviewB.mindcompliance?.connected===false,'compliance summary is tenant isolated');await call('/v1/compliance/overview',{token:a.token,workspace:b.workspace,expect:403});const health=await call('/health',{});assert(health.status==='ok','API remains healthy');
console.log(JSON.stringify({ok:true,workspaceA:a.workspace,workspaceB:b.workspace,triage:overview.relevance,nr1checkMode:handoff.mode,handoffExpiry:handoff.expiresAt,aggregateSummary:{openActions:overview.mindcompliance.summary.openActions,overdueActions:overview.mindcompliance.summary.overdueActions,completionPct:overview.mindcompliance.summary.completionPct},executiveAttention:executive.attention,tenantIsolation:true,privacy:'aggregate_only'},null,2));